program test is

variable x : integer;
variable z : integer;
global variable tmp : bool;

procedure myProc1 : integer(variable val : integer)
    procedure myProc2 : integer(variable val2 : integer)
        variable x : integer;
        begin
            x := 100;
            tmp := putInteger(x);
    end procedure;

    begin
        z:= myProc2(80);
        if (x == 100) then
            return 1;
        end if;
        if (x == 0) then
            return -1; // This should be returned
        end if;

end procedure;

begin
    x := 0;
    z := myProc1(81);
    tmp := putInteger(x);
end program.
