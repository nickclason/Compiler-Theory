program test is
    variable arr : integer[10];
    variable i : integer;
    variable x : integer;
    variable tmp : bool;

    begin
        for (i := 0; i < 10)
            arr[i] := i;
            i := i + 1;
        end for;

        for (i := 0; i < 10)
            tmp := putInteger(arr[i]);
            i := i+1; // without this = infinite loop
        end for;
end program.