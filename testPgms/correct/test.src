program test is

    variable lhsStr : string;
    variable rhsStr : string;

    variable tmp : bool;

begin

    //lhsStr := "Apple";
    //rhsStr := "Apple";

    lhsStr := getString();
    rhsStr := "Apple";

    if (lhsStr == rhsStr) then
        tmp := putInteger(1);
    else
        tmp := putInteger(0);
    end if;

end program.


// Notes

// Comparing these 2 strings works
// lhsStr := "Apple";
// rhsStr := "Apple";

// Comparing these 2 strings works
// lhsStr := getString();
// rhsStr := getString();

// Comparing these 2 strings DOES NOT work
// lhs := "Apple";
// rhs := getString();

// Comparing these 2 strings DOES NOT work
// lhs := getString();
// rhs := "Apple";
