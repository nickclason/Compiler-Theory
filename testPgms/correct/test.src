program test is

    global variable globalString : string;
    variable tmp : bool;
begin

    globalString := "A";
    //globalString := getString();

    if (globalString == "A") then
        tmp := putString("T");
    else
        tmp := putString("F");
    end if;
end program.