program Logicals is

global variable x : bool;
global variable c : string;
global variable tmp : bool;

//global variable i : integer; // my change
//global variable arr : integer[256];
global variable str : string;

begin

x := getBool();
if(x) then
	tmp := putString("T");
else
	tmp := putString("F");
end if;

c := getString();
str := "A";

if(c == "A") then
	tmp := putString("T");
else
	tmp := putString("F");
end if;

end program.
